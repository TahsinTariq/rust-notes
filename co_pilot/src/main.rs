fn main() {
    let n = 101;
    sieve(n);
}

// Funtion: parse_file
// Description: read a file and return the lines containing "the"
// Arguments: name - the name of the file
fn parse_file(name: &str) -> Vec<&str> {
     
    

